# Database connection string for the shared database
DATABASE_URL=postgresql://username:password@localhost:5432/db_name

#please modify this line before running 
#need to modify "username", "password", "localhost", "db_name"
